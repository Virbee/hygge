{{#if user.admin}}
<section class="manage-container">
  {{#if sentences}}
  <h1>Manage Sentences</h1>
  <div id="search-add">
    <label for="search-bar">Search : </label>
    <input type="text" id="search-bar" sentence="sentence" required minlength="4" maxlength="40" size="40">
    <p>or</p>
    <a href="/add/Moto" class="add-button">
      +
    </a>
  </div>

  <div id="table">
    <table class="product-manage-table">
      <thead>
        <tr class="table-row">
          <th colspan="1" class="table-head">Sentence of the Day</th>
          <th colspan="1" class="table-head"> </a> Edit</th>
          <th colspan="1" class="table-head">Delete</th>
        </tr>
      </thead>

      <tbody>
        {{#each sentences}}
        <tr class="table-row">
          <td class="table-division">
            <div>{{this.sentence}}</div>
          </td>
          <td class="table-division"><a href="/product-edit/{{this._id}}" class="fa fa-edit table-icon"><img
                alt="edit logo" src="/media/edit-logo.png" id="edit-logo"></a>
          <td class="table-division"><a href="/delete/Recipe/{{this._id}}" class="fa fa-trash table-icon"
              aria-hidden="true"><img alt="delete logo" src="/media/delete-logo.png" id="delete-logo"></a></td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>

{{/if}}
{{/if}}